models:
  - name: games
    description: "games played in a season"
    config:
      +materialized: table

  - name: plays
    description: "play by play data"
    config:
      +materialized: table

  - name: epa
    description: "epa per play"
    config:
      +materialized: view
